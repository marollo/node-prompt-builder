{
  "name": "node-prompt-builder",
  "version": "0.1.0",
  "description": "A visual node-based prompt builder for Claude",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "litegraph.js": "^0.7.18"
  }
}
